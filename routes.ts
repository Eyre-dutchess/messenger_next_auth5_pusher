// @type {string[]}

export const publicRoutes = ["/"];

export const authRoutes = [
    "/auth/log-in",
    "/auth/register",
    "/auth/error"
]

export const apiAuthPrefix = "/api/auth";

export const DEFAULT_LOGIN_REDIRECT = "/privateRoute/users"